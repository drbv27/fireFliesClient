# Ignorar dependencias de Node.js
# Esta carpeta puede ser muy grande y las dependencias se pueden reinstalar
# ejecutando 'npm install' basado en package.json y package-lock.json.
node_modules/

# Ignorar el archivo de variables de entorno
# ¡MUY IMPORTANTE! Este archivo contiene secretos y claves API.
# NUNCA debe ser subido a un repositorio público o privado.
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos de log
# Si tu aplicación genera archivos de log, usualmente se ignoran.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pm2-logs/ # Si PM2 genera logs en el directorio del proyecto (usualmente no lo hace)
*.pid
*.seed
*.out
*.pid.lock

# Archivos de sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Archivos y carpetas de editores de código e IDEs
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Archivos de configuración de herramientas de desarrollo
.eslintcache
.prettierrc.yaml
.prettierignore

# Archivos de build (si tuvieras un paso de compilación, ej. TypeScript)
# dist/
# build/
# lib/

# Reportes de cobertura de pruebas
coverage/
*.lcov

# Otros archivos temporales o generados
.tmp/
temp/

# Comentarios específicos de dependencias (si alguna genera archivos no deseados)
# ej. .nyc_output

# Archivos de configuración de ngrok (si se guardaran localmente, aunque usualmente son globales)
# ngrok.yml
# .ngrok2/ngrok.yml (usualmente está en el home del usuario, pero por si acaso)